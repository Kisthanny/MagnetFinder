# MagnetFinder 产品路线图

> 本文档描述 MagnetFinder 的演进方向与优先级规划，不代表承诺排期。功能会随用户反馈与维护成本动态调整。

## 愿景

MagnetFinder 要做成一款**轻量、可信、可扩展**的桌面磁力搜索工具：把「找资源 → 筛结果 → 拿磁力」这条链路压缩到最少步骤，同时保持架构清晰，让新搜索源、新交互能力可以持续接入，而不必反复推倒重来。

我们**不内置下载器**，专注做好搜索、筛选、磁力生成与发现；合法性与合规由使用者自行判断。

---

## 当前基线（v0.1.x）

以下能力已落地，构成后续迭代的起点：

- 多源适配器架构（The Pirate Bay 可用，1337X 占位）
- 按清晰度 / 季 / 集筛选，结果按做种数排序
- 磁力链接生成与一键复制
- 定时轮询与新增资源提示
- 五语言界面与亮 / 暗 / 跟随系统主题
- PR 校验（ESLint + typecheck + build）与 GitHub Actions 多平台发布

---

## 路线图分层

按**价值 / 复杂度 / 依赖关系**分为四个层次。同一层内条目无严格先后顺序，可并行推进。

### 一、夯实核心（近期优先）

让「搜索」这件事更完整、更稳。

| 状态 | 功能 | 说明 |
|------|------|------|
| [ ] | **1337X 源适配** | 完成 `x1337` 占位适配器的真实实现，与 Pirate Bay 并列可选、可聚合查询；错误文案已接入 i18n。 |
| [ ] | **独立应用图标** | 为各平台打包提供统一品牌图标（`.icns` / `.ico` / PNG），替换 Electron 默认图标，提升桌面与 Release 页辨识度。 |
| [ ] | **应用内更新** | 集成 `electron-updater`，对接 GitHub Release；启动或菜单中检查新版本，提示下载安装，减少用户手动找 Release 页的摩擦。 |
| [ ] | **桌面通知** | 轮询发现新资源时，除应用内 Toast 外，支持系统级通知（可开关），避免窗口在后台时错过更新。 |
| [ ] | **搜索历史** | 本地记录最近搜索关键词（可清空），下拉或侧栏快速复搜，减少重复输入。 |
| [ ] | **源健康状态** | 列表展示各源最近一次请求是否成功、耗时大致区间；单源失败时不阻断其它源，但让用户知道「哪条路不通」。 |

### 二、体验打磨（中期）

在核心稳定之后，把日常用起来更顺手。

| 状态 | 功能 | 说明 |
|------|------|------|
| [ ] | **快捷键** | 全局或应用内：`Cmd/Ctrl+K` 聚焦搜索框、`Cmd/Ctrl+,` 已有设置入口基础上补充复制、刷新等高频操作。 |
| [ ] | **筛选预设** | 将「源 + 清晰度 + 季/集」保存为命名预设（如「追更单集 1080p」），一键套用。 |
| [ ] | **结果去重增强** | 多源聚合时按 `info_hash` 合并重复条目，展示「出现于哪些源」与最优做种数，避免列表刷屏。 |
| [ ] | **复制历史** | 记录最近复制的磁力链接（条数上限、可清空），便于回溯误关剪贴板的情况。 |
| [ ] | **轮询配置记忆** | 轮询间隔、筛选条件与关键词随场景记忆，重启后恢复上次轮询上下文（可选）。 |
| [ ] | **托盘 / 后台驻留** | 可选最小化到系统托盘，轮询在后台继续；适合长时间等资源的场景。 |

### 三、能力扩展（中长期）

需要更多设计或外部依赖，但可显著拉开与「临时脚本」的差距。

| 状态 | 功能 | 说明 |
|------|------|------|
| [ ] | **更多搜索源** | 在适配器接口稳定前提下，按社区需求接入新源；每源独立开关与健康监控。 |
| [ ] | **元数据辅助搜索** | 可选对接 TMDB 等公开元数据库：输入片名时给出发行年份、海报与规范标题，减少「同名不同片」搜错。 |
| [ ] | **导出结果** | 将当前筛选结果导出为 CSV / JSON（名称、体积、做种、磁力等），方便外部分析或存档。 |
| [ ] | **代理 / 网络设置** | 设置面板配置 HTTP 代理或自定义超时，应对网络环境差异（与主进程 `fetch` 架构天然契合）。 |
| [ ] | **用外部客户端打开** | 不内置下载，但支持「复制磁力」之外的一键唤起系统已安装的 BT 客户端（若平台 API 允许）。 |
| [ ] | **Intel macOS 安装包** | 当前 Release 以 Apple Silicon 为主；评估 universal 或 x64 构建，覆盖 Intel Mac 用户。 |

### 四、质量与生态（持续进行）

不直接面向终端用户，但决定项目能走多远。

| 状态 | 功能 | 说明 |
|------|------|------|
| [ ] | **核心逻辑单测** | 为 `filter.ts`、磁力生成、轮询去重等纯函数补充单元测试，降低重构风险。 |
| [ ] | **E2E 冒烟** | 对 IPC 契约与关键 UI 路径做最小自动化（如 Playwright），防止 Electron 升级回归。 |
| [ ] | **贡献者文档** | 在 README 之外补充「如何加源 / 如何加语言 / 如何发版」的维护者指南，降低协作门槛。 |
| [ ] | **签名与公证** | macOS 公证、Windows 代码签名，减少首次打开时的系统拦截（需开发者证书与 CI 密钥配置）。 |

---

## 明确不做（边界）

以下方向** intentionally out of scope**，避免范围蔓延：

- **内置 BitTorrent 下载客户端** — 与「轻量搜索器」定位冲突，合规与体积成本过高。
- **爬取与索引私有 Tracker** — 仅消费公开源 API / 页面，不做封闭生态穿透。
- **云端账号与跨设备同步** — 保持本地优先，偏好与历史存于 `userData`，无服务器依赖。

---

## 如何参与

1. 在 Issues 中讨论优先级或提交具体需求。
2. 新搜索源、新语言、主题与 i18n 扩展见仓库 `README.md` 中的开发者章节。
3. 发版流程：版本号 bump → `git tag v*` → GitHub Actions 构建 → 发布 Release。

---

*最后更新：与 v0.1.x 功能基线及当前仓库状态对齐。*
